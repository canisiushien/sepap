
export interface ResumerSectorielDepenseData { 
    libelle?: string; 
    avgreel?:number;
    avgprevisionnel?:number;
    periodes?: string[]; 
    previsionnels?: number[]; 
    reels?: number[]; 
}